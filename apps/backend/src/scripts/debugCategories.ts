import mongoose from 'mongoose'; import Category from '../models/category.model'; import { connectMongo, disconnectMongo } from '../config/mongo'; async function checkCats() { await connectMongo(); const cats = await Category.find({}); console.log('Total Categories:', cats.length); cats.forEach(c => console.log(`Name: ${c.name}, Parent: ${c.parentCategory}, Type: ${typeof c.parentCategory}`)); await disconnectMongo(); process.exit(0); } checkCats();
