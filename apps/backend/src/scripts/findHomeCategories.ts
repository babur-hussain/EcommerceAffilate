import mongoose from 'mongoose'; import Category from '../models/category.model'; import { connectMongo, disconnectMongo } from '../config/mongo'; async function findCats() { await connectMongo(); const cats = await Category.find({ name: { $in: ['Groceries', 'Electronics', 'Home & Furniture', 'Home & Kitchen'] } }); cats.forEach(c => console.log(c.name, c._id)); await disconnectMongo(); process.exit(0); } findCats();
